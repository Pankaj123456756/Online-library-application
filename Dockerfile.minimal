# Minimal Dockerfile - Use pre-built JAR
FROM eclipse-temurin:17-jre-alpine

WORKDIR /app

# Copy the pre-built JAR file
COPY backend/target/library-management-1.0.0.jar ./app.jar

# Expose port
EXPOSE 8080

# Run the application
CMD ["java", "-jar", "app.jar"]
